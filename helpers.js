// this is a file of data and helper functions that can be exposed and use in the templating function

exports.moment = require('moment');

// for debugging
exports.dump = (obj) => JSON.stringify(obj, null, 2);
